-- Tailoring CRM — PostgreSQL bootstrap
-- Prisma owns application schema and migrations (Phase 2+).
-- This file only ensures a predictable database exists for local Docker.

SELECT current_database();
